import { describe, expect, test } from "vitest";
describe("validateEnum", () => {
    test("should return true if the value is in the enum", () => {
        
        expect(true).toBe(true);
    });
    test("should return false if the value is not in the enum", () => {
        
        expect(true).toBe(true);
    });
});
    